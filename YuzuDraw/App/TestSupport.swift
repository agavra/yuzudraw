import Foundation

enum AccessibilityID {
    static let newProjectButton = "welcome.new-project"
    static let rectangleToolButton = "toolbar.tool.rectangle"
    static let textToolButton = "toolbar.tool.text"
    static let canvasASCII = "canvas.ascii"
    static let inlineTextEditor = "canvas.inline-text-editor"
}

enum LaunchEnvironmentKey {
    static let uiTesting = "YUZUDRAW_UI_TESTING"
}
